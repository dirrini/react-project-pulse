apiVersion: v1
kind: Service
metadata:
  name: projectpulse-web
  namespace: projectpulse
spec:
  type: ClusterIP
  selector:
    app: projectpulse-web
  ports:
    - name: http
      port: 80
      targetPort: http
